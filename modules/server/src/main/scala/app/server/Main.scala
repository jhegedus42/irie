package app.server

object Main extends App {

  println( "Starting App" )

  // todo-now - make the app "start"
  // steps:
  //  - step 1 : make sbt to start the "App" (this object here, print "hello world")
  //  - step 2 : start the "server" on localhost
  //  - step 3 : serve simple static content (a simple .txt file from the www directory)
  //  - step 4 : serve "the react page"
  //  - step 5 : make SumIntView work in the react page

}
